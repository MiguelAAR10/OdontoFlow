"""Optional Sales Agent process.

The canonical FastAPI application intentionally does not import this package.
Run the agent process separately (for example, ``uvicorn sales_agent.api:app``)
with the ``agent`` optional dependency group installed.
"""

__all__: tuple[str, ...] = ()
